// TODO Phase 4: replace each DoXxx stub below with the full port from
// ScriptCommands/DoXxx.vb (20 files).  Each stub sets IsReady = true so the
// script engine doesn't stall on unimplemented commands.
namespace P3D.ScriptVersion2
{
    public static partial class ScriptCommander
    {
        private static void DoRegister(String s) { IsReady = true; }
        private static void DoScript(String s) { IsReady = true; }
        private static void DoScreen(String s) { IsReady = true; }
        private static void DoPlayer(String s) { IsReady = true; }
        private static void DoMusic(String s) { IsReady = true; }
        private static void DoSound(String s) { IsReady = true; }
        private static void DoEntity(String s) { IsReady = true; }
        private static void DoBattle(String s) { IsReady = true; }
        private static void DoPokemon(String s) { IsReady = true; }
        private static void DoOverworldPokemon(String s) { IsReady = true; }
        private static void DoEnvironment(String s) { IsReady = true; }
        private static void DoText(String s) { IsReady = true; }
        private static void DoOptions(String s) { IsReady = true; }
        private static void DoLevel(String s) { IsReady = true; }
        private static void DoCamera(String s) { IsReady = true; }
        private static void DoItem(String s) { IsReady = true; }
        private static void DoStorage(String s) { IsReady = true; }
        private static void DoNPC(String s) { IsReady = true; }
        private static void DoChat(String s) { IsReady = true; }
        private static void DoDayCare(String s) { IsReady = true; }
        private static void DoPokedex(String s) { IsReady = true; }
        private static void DoRadio(String s) { IsReady = true; }
        private static void DoSystem(String s) { IsReady = true; }
        private static void DoTitle(String s) { IsReady = true; }
    }
}
